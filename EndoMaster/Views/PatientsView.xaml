<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="EndoMaster.Views.PatientsView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:local="using:EndoMaster.Views" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d">
    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="Lista pacjentów" FontSize="22" FontWeight="SemiBold"/>
            <TextBox x:Name="SearchBox" Width="260" PlaceholderText="Wyszukaj" TextChanged="SearchBox_TextChanged"/>
            <Button x:Name="NewPatientBtn" Content="Nowy pacjent" Click="NewPatientBtn_Click"/>
            <Button x:Name="StartCameraBtn" Content="Rozpocznij badanie" Click="StartCameraBtn_Click"/>
        </StackPanel>
        <Grid Grid.Row="1" Padding="8,6" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="#" Grid.Column="0"/>
            <TextBlock Text="Imię" Grid.Column="1"/>
            <TextBlock Text="Nazwisko" Grid.Column="2"/>
            <TextBlock Text="PESEL" Grid.Column="3"/>
            <TextBlock Text="Data badania" Grid.Column="4"/>
        </Grid>
        <ListView x:Name="PatientsList"
          Grid.Row="2"
          SelectionMode="Single"
          SelectionChanged="PatientsList_SelectionChanged"
          DoubleTapped="PatientsList_DoubleTapped">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="220"/>
                        </Grid.ColumnDefinitions>

                        <!-- # -->
                        <TextBlock Grid.Column="0"
                           Text="{Binding Index}"/>

                        <!-- Imię -->
                        <TextBlock Grid.Column="1"
                           Text="{Binding Name}"/>

                        <!-- Nazwisko -->
                        <TextBlock Grid.Column="2"
                           Text="{Binding Surname}"/>

                        <!-- PESEL -->
                        <TextBlock Grid.Column="3"
                           Text="{Binding Pesel}"/>

                        <!-- Data badania (na razie LastExam jest pusty, ale szablon już jest) -->
                        <TextBlock Grid.Column="4"
                           Text="{Binding LastExam}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>